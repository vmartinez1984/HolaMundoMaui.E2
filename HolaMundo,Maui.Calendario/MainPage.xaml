<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HolaMundo.Maui.Calendario.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                 />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                 />

            <Label
                Text="Calendario"
                Style="{StaticResource SubHeadline}"                
                />
            <DatePicker x:Name="DatePicker"/>
            
            
            <Button
                x:Name="CounterBtn"
                Text="Click me"                 
                Clicked="ClickedMostrarFecha"
                HorizontalOptions="Fill" />

            <Label x:Name="Label"/>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
