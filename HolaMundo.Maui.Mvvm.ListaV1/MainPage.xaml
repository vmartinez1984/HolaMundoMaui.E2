<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HolaMundo.Maui.Mvvm.ListaV1.MainPage"
             xmlns:vm="clr-namespace:HolaMundo.Maui.Mvvm.ListaV1.ViewModels"
             >

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a submarine number ten" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Pizzas"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <CollectionView ItemsSource="{Binding Pizzas}" 
                            SelectionMode="Single" 
                            SelectionChanged="SelectionChanged_Pizzas"
                            SelectedItem="{Binding PizzaSeleccionada}"
                            SelectionChangedCommand="{Binding SeleccionarPizzaCommand}"
                            >
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        
                        <StackLayout Orientation="Vertical">
                            <Label Text="{Binding Nombre}"/>
                            <Label Text="{Binding Descripcion}"/>
                            <Label Text="{Binding Precio, StringFormat='$ {0:F2}'}"/>
                        </StackLayout>
                        
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
