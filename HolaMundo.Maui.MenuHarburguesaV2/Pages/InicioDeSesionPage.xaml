<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HolaMundo.Maui.MenuHarburguesa.Pages.InicioDeSesionPage"
             Title="InicioDeSesionPage">
    <ScrollView>
        <VerticalStackLayout
        Padding="30,0"
        Spacing="25">
            <Image
            Source="dotnet_bot.png"
            HeightRequest="185"
            Aspect="AspectFit"
             />

            <Label
            Text="Iniciar sesión"
            Style="{StaticResource Headline}"
             />

            <Label Text="Correo"/>
            <Entry Placeholder="nombre@dominio" x:Name="EntryCorreo" MaxLength="32"/>

            <Label Text="Contraseña"/>
            <Entry Placeholder="*****" x:Name="EntryContrasenia" MaxLength="12" IsPassword="True"/>

            <Button
            x:Name="CounterBtn"
            Text="Iniciar sesión"                 
            Clicked="ClickedIniciarSesion"
            HorizontalOptions="Fill" />

            <ActivityIndicator IsVisible="Visible" x:Name="ActivityIndicator"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>