<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HolaMundo.Maui.Picker.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
            />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
             />

            <Label
                Text="Picker"
                Style="{StaticResource SubHeadline}"
            />

            <Picker Title="Seleccione" x:Name="Picker" ItemDisplayBinding="{Binding Nombre}" SelectedIndexChanged="seleccionarFruta">
                <!--<Picker.Items>
                    <x:String>Platano</x:String>
                    <x:String>Manzana</x:String>
                    <x:String>Pera</x:String>                    
                </Picker.Items>-->
            </Picker>

            <!--<Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />-->
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
